

require.config({
    baseUrl: "./js",
    paths: {
        // the libraries we use
        jquery: [
            '//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min',
            // if the cdn fails load the local copy of jquery
            'lib/jquery'
        ],
        bootstrap: 'lib/bootstrap'
    }
    
    
    
});


